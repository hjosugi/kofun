fn main() -> Int {
    let six: Int = 2 * 3
    let mut value = six
    value = value * 7 - 5
    let floored = -7 // 2
    let remainder = -7 % 2
    return value + floored + remainder + 1 - 2 * 3
}
