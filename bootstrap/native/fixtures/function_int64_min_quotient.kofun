# The one quotient that is not representable. Reachable only now that literals
# reach INT64_MIN, so the guard both backends emit is finally executable.

fn quotient(a: Int, b: Int) -> Int {
    return a // b
}

fn main() {
    let minimum = -9223372036854775807 - 1
    print(quotient(minimum, -1))
}
