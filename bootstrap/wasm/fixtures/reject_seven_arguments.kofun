# A seventh argument has no parameter to bind, and no signature could take it.
fn helper(a: Int, b: Int, c: Int, d: Int, e: Int, f: Int) -> Int {
    return a + b + c + d + e + f
}

fn main() {
    print(helper(1, 2, 3, 4, 5, 6, 7))
}
