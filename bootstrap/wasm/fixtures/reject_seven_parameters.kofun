# The bounded profile stops at six Int parameters.
fn helper(a: Int, b: Int, c: Int, d: Int, e: Int, f: Int, g: Int) -> Int {
    return a + b + c + d + e + f + g
}

fn main() {
    print(helper(1, 2, 3, 4, 5, 6, 7))
}
