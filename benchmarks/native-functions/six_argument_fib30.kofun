fn pick6(a: Int, b: Int, c: Int, d: Int, e: Int, f: Int) -> Int {
    return a + b + c + d + e + f
}

fn drive(n: Int) -> Int {
    if n < 2 {
        return n
    }
    return pick6(drive(n - 1), drive(n - 2), 0, 0, 0, 0)
}

fn main() {
    print(drive(30))
}
