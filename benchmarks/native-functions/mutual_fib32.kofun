fn even_step(n: Int) -> Int {
    if n < 2 {
        return n
    }
    return odd_step(n - 1) + odd_step(n - 2)
}

fn odd_step(n: Int) -> Int {
    if n < 2 {
        return n
    }
    return even_step(n - 1) + even_step(n - 2)
}

fn main() {
    print(even_step(32))
}
