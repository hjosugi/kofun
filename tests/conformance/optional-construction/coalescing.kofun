# `??` stays with #314: it has a single-evaluation and lazy-right-side
# contract this slice does not implement, and a guess here would be the wrong
# place to decide it.

fn main() -> Int {
    let value: Int? = null
    let fallback: Int = value ?? 0
    print(fallback)
    return 0
}
