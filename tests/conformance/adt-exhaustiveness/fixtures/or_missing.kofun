module demo.matching

type Result =
    | Ok(value: Int)
    | Warn(value: Int)
    | Err

fn inspect(value: Result) -> Int {
    let selected: Int = match value {
        Ok(_) | Warn(_) => { 1 }
    }
    return selected
}
