module demo.matching

type Result =
    | Ok(value: Int)
    | Err

fn inspect(value: Result) -> Int {
    let selected: Int = match value {
        Ok(_) if value == value => { 1 },
        Err if value == value => { 0 }
    }
    return selected
}
