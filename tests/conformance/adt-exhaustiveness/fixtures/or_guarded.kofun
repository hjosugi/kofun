module demo.matching

type Result =
    | Ok(value: Int)
    | Warn(value: Int)
    | Err

fn inspect(value: Result) -> Int {
    let selected: Int = match value {
        Ok(_) | Warn(_) if value == value => { 1 },
        Err => { 0 }
    }
    return selected
}
