module demo.matching

type Result =
    | Ok(value: Int)
    | Warn(value: Int)
    | Err

fn inspect(value: Result) -> Int {
    let selected: Int = match value {
        Warn(_) => { 2 },
        Ok(_) | Warn(_) => { 1 },
        Err => { 0 }
    }
    return selected
}
