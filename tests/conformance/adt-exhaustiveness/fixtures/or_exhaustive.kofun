module demo.matching

type Result =
    | Ok(value: Int)
    | Warn(value: Int)
    | Info
    | Err

fn inspect(value: Result) -> Int {
    let selected: Int = match value {
        Ok(item) | Warn(item) => { item },
        (Info | Err) => { 0 }
    }
    return selected
}
