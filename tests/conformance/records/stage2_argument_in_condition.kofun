type Packet = {
    count: Int,
    enabled: Bool,
}

fn score(packet: Packet) -> Int {
    return packet.count
}

fn main() -> Int {
    let packet: Packet = Packet(count: 41, enabled: true)
    if score(packet.count) > 0 {
        print(1)
    }
    return 0
}
