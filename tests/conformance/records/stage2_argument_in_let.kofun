type Packet = {
    count: Int,
    enabled: Bool,
}

fn score(packet: Packet) -> Int {
    return packet.count
}

fn main() -> Int {
    let packet: Packet = Packet(count: 41, enabled: true)
    let total: Int = score(packet.count)
    print(total)
    return 0
}
