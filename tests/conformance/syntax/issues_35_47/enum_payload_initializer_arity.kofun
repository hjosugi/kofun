type Reply =
    | Pending
    | Ready(value: Int)

fn main() -> Int {
    let reply: Reply = Pending(1)
    match reply {
        Pending => { print(0) },
        Ready(value) => { print(value) },
    }
    return 0
}
