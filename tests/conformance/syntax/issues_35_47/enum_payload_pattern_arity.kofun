type Reply =
    | Pending
    | Ready(value: Int)

fn main() -> Int {
    let reply: Reply = Ready(1)
    match reply {
        Pending(unused) => { print(0) },
        Ready(value) => { print(value) },
    }
    return 0
}
