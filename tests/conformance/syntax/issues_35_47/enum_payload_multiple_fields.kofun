type Reply =
    | Pending
    | Ready(left: Int, right: Int)

fn main() -> Int {
    let reply: Reply = Ready(1)
    match reply {
        Pending => { print(0) },
        Ready(value) => { print(value) },
    }
    return 0
}
