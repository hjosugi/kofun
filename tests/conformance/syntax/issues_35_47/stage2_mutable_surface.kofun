# Stage 2 Core executes assignment to a mutable Int binding.

fn main() -> Int {
    let base: Int = 40
    let mut adjustment: Int = 1
    adjustment = adjustment + 1
    print(base + adjustment)
    return 0
}
