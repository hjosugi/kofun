# diagnostic-mode: compile
# expect-code: E2S165
# expect-span: byte 191
fn replace(in text: Int, from old: Int) -> Int {
    return text + old
}

fn main() -> Int {
    print(replace(in: 1, 2))
    return 0
}
