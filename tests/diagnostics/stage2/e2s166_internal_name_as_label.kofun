# diagnostic-mode: compile
# expect-code: E2S166
# expect-span: byte 184
fn replace(in text: Int, from old: Int) -> Int {
    return text + old
}

fn main() -> Int {
    print(replace(text: 1, from: 2))
    return 0
}
